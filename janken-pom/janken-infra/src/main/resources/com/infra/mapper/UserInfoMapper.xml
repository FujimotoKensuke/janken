<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.infra.mapper.UserInfoMapper">
    <insert id="insert">
        INSERT INTO userinfo (id, name, comment, deletedFlg, version)
        VALUES (#{id}, #{name}, #{comment}, #{deletedFlg}, #{version})
    </insert>
    <select id="selectList" resultType="com.infra.entity.UserInfoEntity">
        SELECT id, name, comment, deletedFlg, version FROM userinfo
    </select>
    <update id="update">
        UPDATE userinfo 
        SET id = #{id}, name = #{name}, comment = #{comment}
            , deletedFlg = #{deletedFlg}, version = #{version}
        WHERE id = #{id}
    </update>
    <select id="selectForUpdate" resultType="com.infra.entity.UserInfoEntity">
        SELECT id, name, comment, deletedFlg, version FROM userinfo WHERE id = #{id}
    </select>
</mapper>